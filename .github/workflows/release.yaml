name: Release 

on:
  push:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

  update_dev_branch:
    runs-on: ubuntu-latest
    needs: release
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Push to dev
        run: echo "${{ toJSON(github) }}"

